<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Git 入门：博客备份与日常工作协作</title>
    <link href="/2026/09/17/git/"/>
    <url>/2026/09/17/git/</url>
    
    <content type="html"><![CDATA[<p>Git 不只是写博客时用来备份源码，平时做项目、和同事协作时同样离不开它。没有版本控制，改坏了难回退；多人改同一仓库，也容易互相覆盖。</p><p>本文分两条线：</p><ul><li><strong>个人博客</strong>：本站源码在 <code>hexo</code> 分支，GitHub Pages 在 <code>master</code> 分支。</li><li><strong>工作协作</strong>：常见是从主分支拉功能分支，改完提 PR &#x2F; Merge Request，合并后再上线。</li></ul><hr><h2 id="什么是-Git"><a href="#什么是-Git" class="headerlink" title="什么是 Git"></a>什么是 Git</h2><p>Git 是一个分布式版本控制系统，用来跟踪文件的变化。简单说，它就像给项目里的每个文件装了一台时光机——你可以回到任意历史版本，也能看清每次改了什么、谁改的、为什么改。</p><p>常见用途：</p><ol><li><strong>本地留档</strong>：每次提交都是一个可回退的快照。</li><li><strong>远程备份</strong>：推到 GitHub &#x2F; GitLab 后，换电脑也能把代码拉回来。</li><li><strong>团队协作</strong>：多人并行开发，用分支隔离改动，用 PR 做代码评审再合并。</li></ol><hr><h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><table><thead><tr><th>概念</th><th>含义</th><th>博客例子</th><th>工作例子</th></tr></thead><tbody><tr><td>仓库（Repository）</td><td>被 Git 管理的项目目录</td><td><code>E:\hexo\blog</code></td><td>公司的业务仓库</td></tr><tr><td>提交（Commit）</td><td>一次快照，记录本次改了什么</td><td>「新增文章：Git 入门」</td><td><code>fix: 修复登录态过期</code></td></tr><tr><td>分支（Branch）</td><td>独立的开发线，互不干扰</td><td>源码 <code>hexo</code>，Pages <code>master</code></td><td><code>main</code> + <code>feature/登录页</code></td></tr><tr><td>远程仓库（Remote）</td><td>托管在网上的仓库</td><td><code>SdkjWwb/sdkjwwb.github.io</code></td><td>GitLab &#x2F; GitHub 上的团队仓库</td></tr><tr><td>工作区</td><td>你正在编辑的文件</td><td>刚改完的 <code>git.md</code></td><td>刚改完的某个 <code>.java</code> &#x2F; <code>.ts</code></td></tr><tr><td>暂存区</td><td>已 add、准备提交的改动</td><td><code>git add</code> 之后</td><td>同上</td></tr></tbody></table><p>日常流程可以记成：</p><p><strong>工作区 → add → 暂存区 → commit → 本地仓库 → push → 远程仓库</strong></p><hr><h2 id="安装与首次配置"><a href="#安装与首次配置" class="headerlink" title="安装与首次配置"></a>安装与首次配置</h2><p>到 <a href="https://git-scm.com/">https://git-scm.com</a> 安装 Git。装完后确认：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git -v<br></code></pre></td></tr></table></figure><p>第一次使用前，先配置姓名和邮箱（会出现在提交记录里；公司项目通常要求用公司邮箱）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git config --global user.name <span class="hljs-string">&quot;你的名字&quot;</span><br>git config --global user.email <span class="hljs-string">&quot;你的邮箱@example.com&quot;</span><br></code></pre></td></tr></table></figure><p>查看当前配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git config --list<br></code></pre></td></tr></table></figure><hr><h2 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h2><h3 id="1-初始化仓库"><a href="#1-初始化仓库" class="headerlink" title="1. 初始化仓库"></a>1. 初始化仓库</h3><p>在项目目录下执行一次即可。已有远程仓库时，更常见是直接 <code>clone</code>，而不是本地再 <code>init</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git init<br></code></pre></td></tr></table></figure><h3 id="2-查看状态"><a href="#2-查看状态" class="headerlink" title="2. 查看状态"></a>2. 查看状态</h3><p>最常用。随时看改了什么、哪些还没提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git status<br></code></pre></td></tr></table></figure><h3 id="3-加入暂存区"><a href="#3-加入暂存区" class="headerlink" title="3. 加入暂存区"></a>3. 加入暂存区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git add .<br>git add <span class="hljs-built_in">source</span>/_posts/git.md<br></code></pre></td></tr></table></figure><p>第一条添加所有改动，第二条只添加某个文件。工作里也建议先 <code>status</code>，再按需 <code>add</code>，避免把本地调试文件一并提交。</p><h3 id="4-提交"><a href="#4-提交" class="headerlink" title="4. 提交"></a>4. 提交</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git commit -m <span class="hljs-string">&quot;新增文章：Git 入门&quot;</span><br></code></pre></td></tr></table></figure><p>说明尽量写清楚「做了什么」。工作里更常见带类型前缀，见下文「提交说明怎么写」。</p><h3 id="5-查看历史"><a href="#5-查看历史" class="headerlink" title="5. 查看历史"></a>5. 查看历史</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">log</span> --oneline<br></code></pre></td></tr></table></figure><h3 id="6-关联远程仓库（第一次）"><a href="#6-关联远程仓库（第一次）" class="headerlink" title="6. 关联远程仓库（第一次）"></a>6. 关联远程仓库（第一次）</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git remote add origin https://github.com/SdkjWwb/sdkjwwb.github.io.git<br>git remote -v<br></code></pre></td></tr></table></figure><p>本站已经关联好了，一般只需 <code>git remote -v</code> 确认。公司项目通常是 <code>clone</code> 下来就已经带好 <code>origin</code>。</p><h3 id="7-推送到远程"><a href="#7-推送到远程" class="headerlink" title="7. 推送到远程"></a>7. 推送到远程</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git push -u origin hexo<br>git push<br></code></pre></td></tr></table></figure><p>第一次推某个分支用 <code>-u</code> 建立跟踪；以后在同一分支上直接 <code>git push</code>。工作里推的往往是自己的功能分支，而不是直接推 <code>main</code>。</p><h3 id="8-克隆与拉取"><a href="#8-克隆与拉取" class="headerlink" title="8. 克隆与拉取"></a>8. 克隆与拉取</h3><p>把远程仓库拉到本地：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> -b hexo https://github.com/SdkjWwb/sdkjwwb.github.io.git blog<br>git <span class="hljs-built_in">clone</span> https://github.com/公司组织/项目名.git<br></code></pre></td></tr></table></figure><p>本地已有仓库，同步远程最新改动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git pull<br></code></pre></td></tr></table></figure><p>上班第一件事，很多团队习惯先 <code>git pull</code>（或先 <code>fetch</code> 再合并），再开始改代码。</p><h3 id="9-分支相关"><a href="#9-分支相关" class="headerlink" title="9. 分支相关"></a>9. 分支相关</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">git branch<br>git switch main<br>git switch -c feature/login-page<br></code></pre></td></tr></table></figure><ul><li><code>git branch</code>：看本地有哪些分支，当前在哪一条</li><li><code>git switch main</code>：切回主分支（有的仓库叫 <code>master</code>）</li><li><code>git switch -c ...</code>：新建并切换到新分支</li></ul><p>旧写法 <code>git checkout</code> 也能切分支，新版本更推荐 <code>git switch</code>。</p><hr><h2 id="工作中怎么用-Git"><a href="#工作中怎么用-Git" class="headerlink" title="工作中怎么用 Git"></a>工作中怎么用 Git</h2><h3 id="团队协作日常流程"><a href="#团队协作日常流程" class="headerlink" title="团队协作日常流程"></a>团队协作日常流程</h3><p>多数公司项目不会直接在 <code>main</code> &#x2F; <code>master</code> 上改，而是：</p><ol><li>切到主分支，拉最新代码</li><li>新建功能分支</li><li>改代码、自测</li><li>提交并推到远程</li><li>在 GitHub &#x2F; GitLab 提 Pull Request &#x2F; Merge Request</li><li>同事 Review 通过后合并进主分支</li></ol><p>对应命令大致是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">git switch main<br>git pull<br>git switch -c feature/login-page<br></code></pre></td></tr></table></figure><p>改完后：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">git status<br>git add .<br>git commit -m <span class="hljs-string">&quot;feat: 增加登录页&quot;</span><br>git push -u origin feature/login-page<br></code></pre></td></tr></table></figure><p>然后到网页上创建 PR。合并完成后，本地主分支再拉一次最新：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git switch main<br>git pull<br></code></pre></td></tr></table></figure><h3 id="分支怎么命名"><a href="#分支怎么命名" class="headerlink" title="分支怎么命名"></a>分支怎么命名</h3><p>常见约定（团队可能略有不同，以团队规范为准）：</p><table><thead><tr><th>前缀</th><th>用途</th><th>例子</th></tr></thead><tbody><tr><td><code>feature/</code></td><td>新功能</td><td><code>feature/login-page</code></td></tr><tr><td><code>fix/</code></td><td>修 bug</td><td><code>fix/order-null</code></td></tr><tr><td><code>hotfix/</code></td><td>线上紧急修复</td><td><code>hotfix/pay-crash</code></td></tr><tr><td><code>chore/</code></td><td>杂项、工具、依赖</td><td><code>chore/upgrade-deps</code></td></tr></tbody></table><h3 id="提交说明怎么写"><a href="#提交说明怎么写" class="headerlink" title="提交说明怎么写"></a>提交说明怎么写</h3><p>个人博客可以写「新增 Git 入门」。工作里更建议短句说清意图，很多团队用约定式提交：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">feat: 增加导出 Excel<br>fix: 修复登录态过期<br>docs: 补充接口说明<br>refactor: 拆分订单查询逻辑<br>chore: 更新依赖版本<br></code></pre></td></tr></table></figure><p>原则是：<strong>让别人只看提交记录，也知道这次改动解决了什么问题</strong>。</p><h3 id="多人协作注意点"><a href="#多人协作注意点" class="headerlink" title="多人协作注意点"></a>多人协作注意点</h3><ol><li><strong>先拉再改</strong>：动手前先 <code>git pull</code>，减少和别人的改动撞车。</li><li><strong>不要直接推主分支</strong>：多数团队要求走 PR，主分支有保护规则。</li><li><strong>一个分支一件事</strong>：登录页和修订单 bug 分开两条分支，Review 更清晰。</li><li><strong>出现冲突时</strong>：<code>git pull</code> 或合并时 Git 会标出冲突文件。打开文件，处理 <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code> &#x2F; <code>=======</code> &#x2F; <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> 标记，改完后再：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git add .<br>git commit -m <span class="hljs-string">&quot;resolve: 合并冲突&quot;</span><br></code></pre></td></tr></table></figure><p>入门阶段先掌握「手动解决冲突再提交」即可；<code>rebase</code> 等进阶操作以后再学。</p><h3 id="博客用法-vs-工作用法"><a href="#博客用法-vs-工作用法" class="headerlink" title="博客用法 vs 工作用法"></a>博客用法 vs 工作用法</h3><table><thead><tr><th>场景</th><th>常见做法</th></tr></thead><tbody><tr><td>个人博客</td><td>多在 <code>hexo</code> 上直接改、提交、<code>push</code></td></tr><tr><td>公司项目</td><td>功能分支开发 + PR 评审，合并进 <code>main</code></td></tr><tr><td>个人博客</td><td>自己对质量负责，流程可以简单</td></tr><tr><td>公司项目</td><td>还要考虑 Review、CI、发布节奏</td></tr></tbody></table><p>命令是同一套，差别主要在<strong>分支策略和协作流程</strong>。</p><hr><h2 id="本站实际用法（博客）"><a href="#本站实际用法（博客）" class="headerlink" title="本站实际用法（博客）"></a>本站实际用法（博客）</h2><p>本仓库约定：</p><table><thead><tr><th>分支</th><th>放什么</th></tr></thead><tbody><tr><td><code>hexo</code></td><td>Hexo 源码：文章、配置、主题</td></tr><tr><td><code>master</code></td><td><code>hexo deploy</code> 生成后的静态网页</td></tr></tbody></table><p>改完文章或配置后，备份源码一般是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">git status<br>git add .<br>git commit -m <span class="hljs-string">&quot;Backup hexo source&quot;</span><br>git push origin hexo<br></code></pre></td></tr></table></figure><p>如果当前已经在 <code>hexo</code> 且设置了上游，最后一步也可以简写成 <code>git push</code>。</p><hr><h2 id="和-Hexo-部署的关系"><a href="#和-Hexo-部署的关系" class="headerlink" title="和 Hexo 部署的关系"></a>和 Hexo 部署的关系</h2><p>这两件事不要混：</p><table><thead><tr><th>操作</th><th>作用</th></tr></thead><tbody><tr><td><code>git push origin hexo</code></td><td>把源码备份到 GitHub 的 <code>hexo</code> 分支</td></tr><tr><td><code>hexo deploy</code></td><td>把生成好的网页推到 <code>master</code>，更新站点</td></tr></tbody></table><p>只 <code>git push</code> 不会更新 <a href="https://sdkjwwb.github.io/">https://sdkjwwb.github.io</a> 上的页面；只 <code>hexo deploy</code> 也不会把 <code>_posts</code> 源码备份到 <code>hexo</code> 分支。日常建议两边都做：</p><ol><li>本地 <code>hexo s</code> 预览没问题</li><li><code>hexo clean &amp;&amp; hexo g -d</code> 部署网页</li><li><code>git add</code> → commit → <code>push origin hexo</code> 备份源码</li></ol><hr><h2 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h2><ol><li><strong>先看状态再提交</strong>：养成 <code>git status</code> 的习惯，确认暂存的文件是你想提交的。</li><li><strong>分清场景</strong>：博客源码在 <code>hexo</code>；公司项目先弄清主分支叫 <code>main</code> 还是 <code>master</code>，以及能不能直接推。</li><li><strong>别提交不该提交的东西</strong>：博客的 <code>.gitignore</code> 已忽略 <code>node_modules/</code>、<code>public/</code>、<code>db.json</code>；工作项目里也不要提交密钥、本地配置、构建产物。</li><li><strong>提交说明写清楚</strong>：博客如「新增 Git 入门」；工作如 <code>fix: 修复登录态过期</code>。</li><li><strong>不要轻易 force push</strong>：尤其是已经推到远程、别人可能基于它开发的分支。</li></ol><hr><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>先记住这条主链路：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">git status<br>git add .<br>git commit -m <span class="hljs-string">&quot;说明这次改了什么&quot;</span><br>git push<br></code></pre></td></tr></table></figure><p>再按场景补两招：</p><ul><li><strong>博客</strong>：在 <code>hexo</code> 分支备份源码，需要更新站点时再 <code>hexo deploy</code></li><li><strong>工作</strong>：从 <code>main</code> 拉功能分支，推远程后提 PR，合并后再回到主分支 <code>pull</code></li></ul><p>把 <code>pull</code>、<code>switch</code>、<code>log --oneline</code> 也用熟，日常开发和协作就基本够用了。</p>]]></content>
    
    
    <categories>
      
      <category>学习</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Git</tag>
      
      <tag>教程</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>恢复 Hexo 博客</title>
    <link href="/2026/09/17/%E6%81%A2%E5%A4%8Dhexo%E5%8D%9A%E5%AE%A2/"/>
    <url>/2026/09/17/%E6%81%A2%E5%A4%8Dhexo%E5%8D%9A%E5%AE%A2/</url>
    
    <content type="html"><![CDATA[<p>换电脑、重装系统，或本地目录弄丢之后，可以按下面两种方式把博客找回来。</p><ul><li><strong>方法一</strong>：全新环境从零安装 Hexo，再装主题、改配置、部署。适合第一次搭建，或 GitHub 上没有源码备份。</li><li><strong>方法二</strong>：从 GitHub 的 <code>hexo</code> 分支把源码拉回来。本站已经把源码备份在仓库里，<strong>优先用这种方法</strong>。</li></ul><p>本站当前信息，方便对照：</p><table><thead><tr><th>项目</th><th>值</th></tr></thead><tbody><tr><td>站点地址</td><td><a href="https://sdkjwwb.github.io/">https://sdkjwwb.github.io</a></td></tr><tr><td>仓库</td><td><a href="https://github.com/SdkjWwb/sdkjwwb.github.io.git">SdkjWwb&#x2F;sdkjwwb.github.io</a></td></tr><tr><td>源码分支</td><td><code>hexo</code></td></tr><tr><td>部署分支</td><td><code>master</code>（GitHub Pages 实际访问的静态文件）</td></tr><tr><td>Hexo 版本</td><td>8.x</td></tr><tr><td>主题</td><td><a href="https://github.com/fluid-dev/hexo-theme-fluid">hexo-theme-fluid</a> 1.9.9</td></tr></tbody></table><hr><h2 id="方法一：全新环境从零搭建"><a href="#方法一：全新环境从零搭建" class="headerlink" title="方法一：全新环境从零搭建"></a>方法一：全新环境从零搭建</h2><h3 id="1-安装-Node-js-和-Git"><a href="#1-安装-Node-js-和-Git" class="headerlink" title="1. 安装 Node.js 和 Git"></a>1. 安装 Node.js 和 Git</h3><p>Hexo 依赖 Node.js，主题和部署都依赖 Git。</p><ul><li>Node.js：到 <a href="https://nodejs.org/">https://nodejs.org</a> 安装 LTS 版本。装完后确认：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">node -v<br>npm -v<br></code></pre></td></tr></table></figure><ul><li>Git：到 <a href="https://git-scm.com/">https://git-scm.com</a> 安装。装完后确认：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git -v<br></code></pre></td></tr></table></figure><p>Windows 上建议安装时勾选把 Git 加入 PATH，之后在 PowerShell 或 Git Bash 里都能用。</p><h3 id="2-安装-Hexo-CLI"><a href="#2-安装-Hexo-CLI" class="headerlink" title="2. 安装 Hexo CLI"></a>2. 安装 Hexo CLI</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install hexo-cli -g<br>hexo -v<br></code></pre></td></tr></table></figure><p>注意包名是 <code>hexo-cli</code>，中间没有空格。不要写成 <code>hexo -cli</code>。</p><h3 id="3-初始化站点"><a href="#3-初始化站点" class="headerlink" title="3. 初始化站点"></a>3. 初始化站点</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo init blog<br><span class="hljs-built_in">cd</span> blog<br>npm install<br></code></pre></td></tr></table></figure><p><code>hexo init</code> 会生成站点骨架，包括 <code>_config.yml</code>、<code>source/_posts/</code>、<code>themes/</code> 等。</p><p>本仓库已经是初始化好的站点，换机后一般不需要再执行 <code>hexo init</code>，直接看方法二。</p><h3 id="4-安装-Fluid-主题"><a href="#4-安装-Fluid-主题" class="headerlink" title="4. 安装 Fluid 主题"></a>4. 安装 Fluid 主题</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> themes<br>git <span class="hljs-built_in">clone</span> https://github.com/fluid-dev/hexo-theme-fluid.git hexo-theme-fluid<br></code></pre></td></tr></table></figure><p>克隆完成后，编辑站点根目录的 <code>_config.yml</code>，把主题改成：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">theme:</span> <span class="hljs-string">hexo-theme-fluid</span><br></code></pre></td></tr></table></figure><p>主题自己的配置在 <code>themes/hexo-theme-fluid/_config.yml</code>。站点标题、链接、部署等改根目录的 <code>_config.yml</code>；外观、导航、评论等改主题配置。</p><h3 id="5-调整站点配置"><a href="#5-调整站点配置" class="headerlink" title="5. 调整站点配置"></a>5. 调整站点配置</h3><p>根目录 <code>_config.yml</code> 至少确认这几项：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">title:</span> <span class="hljs-string">我的博客</span><br><span class="hljs-attr">author:</span> <span class="hljs-string">wangwenbiao</span><br><span class="hljs-attr">language:</span> <span class="hljs-string">zh-CN</span><br><span class="hljs-attr">timezone:</span> <span class="hljs-string">Asia/Shanghai</span><br><span class="hljs-attr">url:</span> <span class="hljs-string">https://sdkjwwb.github.io</span><br><br><span class="hljs-attr">theme:</span> <span class="hljs-string">hexo-theme-fluid</span><br><br><span class="hljs-attr">deploy:</span><br>  <span class="hljs-attr">type:</span> <span class="hljs-string">git</span><br>  <span class="hljs-attr">repo:</span> <span class="hljs-string">https://github.com/SdkjWwb/sdkjwwb.github.io.git</span><br>  <span class="hljs-attr">branch:</span> <span class="hljs-string">master</span><br></code></pre></td></tr></table></figure><p>部署插件需要单独安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install hexo-deployer-git --save<br></code></pre></td></tr></table></figure><h3 id="6-本地预览"><a href="#6-本地预览" class="headerlink" title="6. 本地预览"></a>6. 本地预览</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean<br>hexo generate<br>hexo server<br></code></pre></td></tr></table></figure><p>浏览器打开 <a href="http://localhost:4000/">http://localhost:4000</a>。<code>hexo server</code> 也可以简写成 <code>hexo s</code>。</p><h3 id="7-部署到-GitHub-Pages"><a href="#7-部署到-GitHub-Pages" class="headerlink" title="7. 部署到 GitHub Pages"></a>7. 部署到 GitHub Pages</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo deploy<br></code></pre></td></tr></table></figure><p>这条命令会把 <code>public/</code> 推到仓库的 <code>master</code> 分支。源码继续留在 <code>hexo</code> 分支，不要把源码和生成后的静态文件混在同一个分支里。</p><hr><h2 id="方法二：从-GitHub-备份恢复（推荐）"><a href="#方法二：从-GitHub-备份恢复（推荐）" class="headerlink" title="方法二：从 GitHub 备份恢复（推荐）"></a>方法二：从 GitHub 备份恢复（推荐）</h2><p>本仓库已经把 Hexo 源码备份在 <code>hexo</code> 分支上，换机后按下面做即可。</p><h3 id="1-克隆源码分支"><a href="#1-克隆源码分支" class="headerlink" title="1. 克隆源码分支"></a>1. 克隆源码分支</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> -b hexo https://github.com/SdkjWwb/sdkjwwb.github.io.git blog<br><span class="hljs-built_in">cd</span> blog<br></code></pre></td></tr></table></figure><p>如果已经克隆了仓库但当前在 <code>master</code> 上，切换到源码分支：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git checkout hexo<br></code></pre></td></tr></table></figure><h3 id="2-安装依赖"><a href="#2-安装依赖" class="headerlink" title="2. 安装依赖"></a>2. 安装依赖</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install<br></code></pre></td></tr></table></figure><p><code>package.json</code> 里已经包含 Hexo 8、渲染器、以及 <code>hexo-deployer-git</code>。一般不需要再全局安装一遍 <code>hexo-cli</code>，用项目本地的命令即可：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npx hexo version<br></code></pre></td></tr></table></figure><h3 id="3-确认主题还在"><a href="#3-确认主题还在" class="headerlink" title="3. 确认主题还在"></a>3. 确认主题还在</h3><p>主题目录应是 <code>themes/hexo-theme-fluid</code>。如果这个目录是空的，重新克隆一次：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> https://github.com/fluid-dev/hexo-theme-fluid.git themes/hexo-theme-fluid<br></code></pre></td></tr></table></figure><p>然后确认根目录 <code>_config.yml</code> 里是：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">theme:</span> <span class="hljs-string">hexo-theme-fluid</span><br></code></pre></td></tr></table></figure><h3 id="4-预览和重新部署"><a href="#4-预览和重新部署" class="headerlink" title="4. 预览和重新部署"></a>4. 预览和重新部署</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">npx hexo clean<br>npx hexo s<br></code></pre></td></tr></table></figure><p>确认本地没问题后再部署：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">npx hexo generate<br>npx hexo deploy<br></code></pre></td></tr></table></figure><p>部署只更新 <code>master</code> 上的静态页面。文章、配置、主题这些源码改完后，还要提交并推送到 <code>hexo</code> 分支，下次换机才能继续用方法二恢复：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">git add .<br>git commit -m <span class="hljs-string">&quot;Backup hexo source&quot;</span><br>git push origin hexo<br></code></pre></td></tr></table></figure><hr><h2 id="文章头部（Front-matter）"><a href="#文章头部（Front-matter）" class="headerlink" title="文章头部（Front-matter）"></a>文章头部（Front-matter）</h2><p>每篇文章开头必须用两行 <code>---</code> 包住 YAML。中间是元数据，后面才是正文。推荐这样写：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-meta">---</span><br><span class="hljs-attr">title:</span> <span class="hljs-string">我的第一篇文章</span><br><span class="hljs-attr">date:</span> <span class="hljs-number">2025-08-16 15:00:00</span><br><span class="hljs-attr">categories:</span> <span class="hljs-string">学习</span><br><span class="hljs-attr">tags:</span> [<span class="hljs-string">Hexo</span>, <span class="hljs-string">教程</span>]<br><span class="hljs-attr">sticky:</span> <span class="hljs-number">1</span><br><span class="hljs-meta">---</span><br></code></pre></td></tr></table></figure><p>本站用的是 Fluid 主题，有几个常见字段和 Butterfly 不一样，直接照搬会不生效：</p><table><thead><tr><th>字段</th><th>作用</th><th>本站能不能用</th></tr></thead><tbody><tr><td><code>title</code> &#x2F; <code>date</code> &#x2F; <code>categories</code> &#x2F; <code>tags</code></td><td>标题、时间、分类、标签</td><td>能，Hexo 自带</td></tr><tr><td><code>sticky: 1</code></td><td>首页置顶，数字越大越靠前</td><td>能。Fluid 认的是 <code>sticky</code>，不是 <code>top</code></td></tr><tr><td><code>top: true</code></td><td>部分主题的置顶写法</td><td>本站无效</td></tr><tr><td><code>abbrlink: 1234</code></td><td>固定短链接</td><td>需要先装 <code>hexo-abbrlink</code>，目前没装</td></tr><tr><td><code>password: 123</code></td><td>文章加密</td><td>需要先装 <code>hexo-blog-encrypt</code>，目前没装</td></tr></tbody></table><p><code>tags: [Hexo, 教程]</code> 这种一行写法是合法 YAML。不要漏掉 front-matter 结尾那一行 <code>---</code>，否则 Hexo 会把后面的表格、标题都当成 YAML，直接报错。</p><p>新建文章：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo new <span class="hljs-string">&quot;文章标题&quot;</span><br></code></pre></td></tr></table></figure><p>生成的文件在 <code>source/_posts/</code> 里，把头部改成上面的格式，再写正文即可。</p><hr><h2 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h2><table><thead><tr><th>命令</th><th>作用</th></tr></thead><tbody><tr><td><code>hexo new &quot;文章标题&quot;</code></td><td>新建文章</td></tr><tr><td><code>hexo s</code></td><td>本地预览</td></tr><tr><td><code>hexo g</code></td><td>生成静态文件</td></tr><tr><td><code>hexo d</code></td><td>部署到 GitHub Pages</td></tr><tr><td><code>hexo clean</code></td><td>清除缓存和 <code>public/</code></td></tr></tbody></table><p><code>hexo g</code> 和 <code>hexo d</code> 可以连写：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean &amp;&amp; hexo g -d<br></code></pre></td></tr></table></figure><hr><h2 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h2><ol><li><strong>分清两个分支</strong>：<code>hexo</code> 放源码，<code>master</code> 放生成后的网页。恢复博客时一定要检出 <code>hexo</code>。</li><li><strong>主题路径</strong>：是 <code>themes/</code>，不是 <code>theme/</code>。</li><li><strong>front-matter 必须闭合</strong>：开头和元数据结束后各有一行 <code>---</code>。<code>tags: [Hexo, 教程]</code> 这种写法可以，但结尾的 <code>---</code> 不能少。</li><li><strong>先本地预览再部署</strong>：<code>hexo s</code> 没问题后再 <code>hexo d</code>，避免把坏掉的页面推上网。</li></ol>]]></content>
    
    
    <categories>
      
      <category>学习</category>
      
    </categories>
    
    
    <tags>
      
      <tag>教程</tag>
      
      <tag>Hexo</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2026/09/17/hello-world/"/>
    <url>/2026/09/17/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
